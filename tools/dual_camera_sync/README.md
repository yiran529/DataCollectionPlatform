# 双摄像头同步捕获

DECXIN 立体相机 (device 6) 和 DECXIN 单目相机 (device 4) 同步捕获工具。

## 快速开始

```bash
# 带偏移补偿录制（推荐）
python dual_camera_capture.py -p -t 10

# 测量原始偏移（不补偿）
python dual_camera_capture.py -p -t 30 --no-offset
```

## 偏移补偿原理

由于两个相机通过不同 USB 端口读取，存在固定的时间偏移（约 756ms）。程序会自动补偿这个偏移。

### 校准流程

1. **测量原始偏移**
```bash
python dual_camera_capture.py -t 30 --no-offset
```

2. **分析时间戳**
```bash
python analyze_timestamps.py sync_test/capture_xxx/timestamps.txt
```

3. **更新偏移值**（如果需要）
```bash
# 使用自定义偏移
python dual_camera_capture.py --offset 760.0 -p
```

## 参数说明

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--stereo-device` | 6 | 立体相机设备ID |
| `--mono-device` | 4 | 单目相机设备ID |
| `--offset` | 756.0 | 时间偏移补偿(ms) |
| `--no-offset` | False | 不应用偏移补偿 |
| `-t, --duration` | 10 | 录制时长(秒) |
| `-p, --preview` | False | 显示实时预览 |

## 输出文件

```
sync_test/capture_YYYYMMDD_HHMMSS/
├── sync_video.mp4      # 拼接视频
└── timestamps.txt      # 时间戳记录
```

### timestamps.txt 格式
```
# offset_ms=756.0
# frame_idx, stereo_timestamp, mono_timestamp, raw_diff_ms, corrected_diff_ms
0, 1234567890.123, 1234567890.125, 750.0, -6.0
```

- `raw_diff_ms`: 原始时间差
- `corrected_diff_ms`: 补偿后时间差（应接近0）

## 视频布局

```
+------------------+--------+
|    左图 (L)      |        |
+------------------+  单目  |
|    右图 (R)      |        |
+------------------+--------+
```

## 同步质量判定

- ✅ 良好: 平均误差 < 20ms
- 🔶 一般: 平均误差 < 50ms  
- ❌ 较差: 需重新校准或硬件同步

